using UnityEngine;

public class Settings : MonoBehaviour
{

    // Use this for initialization
    void Awake()
    {
        LoginHelper.Login();
    }

    // Update is called once per frame
    void Update()
    {
    }
}
